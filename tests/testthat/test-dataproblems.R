context("Data problems")
# data(phone, package = "flipExampleData")
# levs <- attr(phone$q3, "value.labels")
# as.matrix(levs)
#
# z <- phone$q3
# z[is.na(z)] <- 100
# z <- as.numeric(z)
# z <- factor(z)
# #z <- as.integer(unclass(z))
#
# lv <- c("-9", "0", names(levs[7:1]), "100")
# levels(z) <- lv
# z[z == "100"] <- NA
# phone$q3 <- z
# xtabs(~q3 + q2, data = phone)
#
# Regression(q3 ~ q2, data = phone)
#
#
# table(phone$q2)
#
#
